let a = document.body.childNodes[1]
console.log(a);

console.log(a.parentNode)// accessing the parent node

//* $0 and $1 is used to console the current tag of html in browser console on which is clicked